{
  "name": "Sheet to Layers",
  "id": "com.spreadsheet.sync.figma",
  "api": "1.0.0",
  "editorType": ["figma"],
  "main": "dist/code.js",
  "ui": "dist/ui.html",
  "networkAccess": {
    "allowedDomains": ["*"],
    "reasoning": "The plugin fetches user-provided CSV files from arbitrary URLs and accesses Google Sheets API endpoints to retrieve spreadsheet data. Allowing all domains ensures users can sync from any hosted CSV or image link without needing to whitelist each domain."
  }
}

